# Bu dosya yalnızca akıllı sözleşmeyi sıfırdan derleyip Polygon ağına
# deploy etmek isteyenler için gereklidir. Uygulamanın çalışması için
# GEREKLİ DEĞİLDİR (ABI zaten contracts/OpticalFormRegistry.abi.json
# içinde hazır biçimde paketlenmiştir).

py-solc-x==2.0.3

# Birim/entegrasyon testlerini çalıştırmak için (tests/ klasörü).
pytest>=8.0.0

# tests/test_blockchain_service.py'nin yerel EVM üzerinde gerçek sözleşme
# testleri için (eth-tester / py-evm). Kurulu değilse o testler atlanır (skip),
# diğer testler normal çalışır.
web3[tester]>=7.0.0
eth-tester>=0.13.0
py-evm>=0.12.0

# Akıllı sözleşmenin statik güvenlik taraması için (bkz. contracts/SLITHER_REPORT.md).
slither-analyzer>=0.11.0
